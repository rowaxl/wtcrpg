import React from 'react';
import { MenuHeader } from 'semantic-ui-react'

export default class Toolbar extends React.Component {
    render() {
        return (
            <MenuHeader>This is Toolbar</MenuHeader>
        )
    }
}